# Simulation mode - faster, no real hardware
robot_status_publisher:
  ros__parameters:
    use_sim_time: true  # ROS 2 simulation time
    robot_id: "SIM-TEST-001"
    robot_type: "simulation"
    zone_id: "GAZEBO-WAREHOUSE"
    fleet_number: 999
    status_rate_hz: 10.0  # Fast for testing
    max_payload_kg: 100.0
    battery_level: 100.0
    current_location: "SIM-ORIGIN"
    assigned_task: "SIMULATION-TEST"

zone_coordinator:
  ros__parameters:
    use_sim_time: true
    robot_id: "SIM-TEST-001"
    zone_id: "GAZEBO-WAREHOUSE"
    robot_type: "simulation"
    max_capacity: 100.0

task_queue_action:
  ros__parameters:
    use_sim_time: true
    robot_id: "SIM-TEST-001"
    robot_type: "simulation"
    zone_id: "GAZEBO-WAREHOUSE"
    max_items_per_trip: 500
    battery_level: 100.0
    picking_speed_items_per_min: 50.0
