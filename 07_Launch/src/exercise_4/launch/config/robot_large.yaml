# Large robot configuration - Heavy-duty industrial transport
robot_status_publisher:
  ros__parameters:
    robot_id: "AMR-HEAVY-TRANSPORT-L01"
    robot_type: "transport"
    zone_id: "WAREHOUSE-MAIN-LOADING"
    fleet_number: 100
    status_rate_hz: 1.0  # Slower updates for stable transport
    max_payload_kg: 1500.0  # Heavy industrial capacity
    battery_level: 78.0
    current_location: "LOADING-DOCK-MAIN-3"
    assigned_task: "PALLET-MOVE-DOCK3-TO-STORAGE-A"

zone_coordinator:
  ros__parameters:
    robot_id: "AMR-HEAVY-TRANSPORT-L01"
    zone_id: "WAREHOUSE-MAIN-LOADING"
    robot_type: "transport"
    max_capacity: 1500.0

task_queue_action:
  ros__parameters:
    robot_id: "AMR-HEAVY-TRANSPORT-L01"
    robot_type: "transport"
    zone_id: "WAREHOUSE-MAIN-LOADING"
    max_items_per_trip: 1000
    battery_level: 78.0
    picking_speed_items_per_min: 5.0
